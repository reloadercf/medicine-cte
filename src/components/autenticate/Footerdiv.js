import React, { Fragment } from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const Footerdiv = () => (
  <Fragment>
    <Footer style={{ textAlign: 'center' }}>
      Ant Design ©2021 Created for Sandy
    </Footer>
  </Fragment>
);

export default Footerdiv;
